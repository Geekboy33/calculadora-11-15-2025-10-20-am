# =============================================================================
# DAES ETH USD Contracts - Environment Configuration
# =============================================================================
# Rename this file to .env and fill in your values
# NEVER commit .env to git!

# --- Ethereum RPC (Alchemy) ---
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>
ETH_SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>

# --- Deployer (pays gas for deployment) ---
DEPLOYER_PRIVATE_KEY=0x<YOUR_DEPLOYER_PRIVATE_KEY>

# --- Admin Address (receives admin roles) ---
ADMIN_ADDRESS=0x<YOUR_ADMIN_ADDRESS>

# --- DAES Signer (signs EIP-712 authorizations) ---
DAES_SIGNER_ADDRESS=0x<YOUR_DAES_SIGNER_ADDRESS>

# --- Etherscan API Key (for contract verification) ---
ETHERSCAN_API_KEY=<YOUR_ETHERSCAN_API_KEY>

# --- Gas Reporter (optional) ---
REPORT_GAS=false

# DAES ETH USD Contracts - Environment Configuration
# =============================================================================
# Rename this file to .env and fill in your values
# NEVER commit .env to git!

# --- Ethereum RPC (Alchemy) ---
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>
ETH_SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>

# --- Deployer (pays gas for deployment) ---
DEPLOYER_PRIVATE_KEY=0x<YOUR_DEPLOYER_PRIVATE_KEY>

# --- Admin Address (receives admin roles) ---
ADMIN_ADDRESS=0x<YOUR_ADMIN_ADDRESS>

# --- DAES Signer (signs EIP-712 authorizations) ---
DAES_SIGNER_ADDRESS=0x<YOUR_DAES_SIGNER_ADDRESS>

# --- Etherscan API Key (for contract verification) ---
ETHERSCAN_API_KEY=<YOUR_ETHERSCAN_API_KEY>

# --- Gas Reporter (optional) ---
REPORT_GAS=false

# DAES ETH USD Contracts - Environment Configuration
# =============================================================================
# Rename this file to .env and fill in your values
# NEVER commit .env to git!

# --- Ethereum RPC (Alchemy) ---
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>
ETH_SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>

# --- Deployer (pays gas for deployment) ---
DEPLOYER_PRIVATE_KEY=0x<YOUR_DEPLOYER_PRIVATE_KEY>

# --- Admin Address (receives admin roles) ---
ADMIN_ADDRESS=0x<YOUR_ADMIN_ADDRESS>

# --- DAES Signer (signs EIP-712 authorizations) ---
DAES_SIGNER_ADDRESS=0x<YOUR_DAES_SIGNER_ADDRESS>

# --- Etherscan API Key (for contract verification) ---
ETHERSCAN_API_KEY=<YOUR_ETHERSCAN_API_KEY>

# --- Gas Reporter (optional) ---
REPORT_GAS=false

# DAES ETH USD Contracts - Environment Configuration
# =============================================================================
# Rename this file to .env and fill in your values
# NEVER commit .env to git!

# --- Ethereum RPC (Alchemy) ---
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>
ETH_SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>

# --- Deployer (pays gas for deployment) ---
DEPLOYER_PRIVATE_KEY=0x<YOUR_DEPLOYER_PRIVATE_KEY>

# --- Admin Address (receives admin roles) ---
ADMIN_ADDRESS=0x<YOUR_ADMIN_ADDRESS>

# --- DAES Signer (signs EIP-712 authorizations) ---
DAES_SIGNER_ADDRESS=0x<YOUR_DAES_SIGNER_ADDRESS>

# --- Etherscan API Key (for contract verification) ---
ETHERSCAN_API_KEY=<YOUR_ETHERSCAN_API_KEY>

# --- Gas Reporter (optional) ---
REPORT_GAS=false

# DAES ETH USD Contracts - Environment Configuration
# =============================================================================
# Rename this file to .env and fill in your values
# NEVER commit .env to git!

# --- Ethereum RPC (Alchemy) ---
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>
ETH_SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>

# --- Deployer (pays gas for deployment) ---
DEPLOYER_PRIVATE_KEY=0x<YOUR_DEPLOYER_PRIVATE_KEY>

# --- Admin Address (receives admin roles) ---
ADMIN_ADDRESS=0x<YOUR_ADMIN_ADDRESS>

# --- DAES Signer (signs EIP-712 authorizations) ---
DAES_SIGNER_ADDRESS=0x<YOUR_DAES_SIGNER_ADDRESS>

# --- Etherscan API Key (for contract verification) ---
ETHERSCAN_API_KEY=<YOUR_ETHERSCAN_API_KEY>

# --- Gas Reporter (optional) ---
REPORT_GAS=false

# DAES ETH USD Contracts - Environment Configuration
# =============================================================================
# Rename this file to .env and fill in your values
# NEVER commit .env to git!

# --- Ethereum RPC (Alchemy) ---
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>
ETH_SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>

# --- Deployer (pays gas for deployment) ---
DEPLOYER_PRIVATE_KEY=0x<YOUR_DEPLOYER_PRIVATE_KEY>

# --- Admin Address (receives admin roles) ---
ADMIN_ADDRESS=0x<YOUR_ADMIN_ADDRESS>

# --- DAES Signer (signs EIP-712 authorizations) ---
DAES_SIGNER_ADDRESS=0x<YOUR_DAES_SIGNER_ADDRESS>

# --- Etherscan API Key (for contract verification) ---
ETHERSCAN_API_KEY=<YOUR_ETHERSCAN_API_KEY>

# --- Gas Reporter (optional) ---
REPORT_GAS=false

# DAES ETH USD Contracts - Environment Configuration
# =============================================================================
# Rename this file to .env and fill in your values
# NEVER commit .env to git!

# --- Ethereum RPC (Alchemy) ---
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>
ETH_SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>

# --- Deployer (pays gas for deployment) ---
DEPLOYER_PRIVATE_KEY=0x<YOUR_DEPLOYER_PRIVATE_KEY>

# --- Admin Address (receives admin roles) ---
ADMIN_ADDRESS=0x<YOUR_ADMIN_ADDRESS>

# --- DAES Signer (signs EIP-712 authorizations) ---
DAES_SIGNER_ADDRESS=0x<YOUR_DAES_SIGNER_ADDRESS>

# --- Etherscan API Key (for contract verification) ---
ETHERSCAN_API_KEY=<YOUR_ETHERSCAN_API_KEY>

# --- Gas Reporter (optional) ---
REPORT_GAS=false

# DAES ETH USD Contracts - Environment Configuration
# =============================================================================
# Rename this file to .env and fill in your values
# NEVER commit .env to git!

# --- Ethereum RPC (Alchemy) ---
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>
ETH_SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>

# --- Deployer (pays gas for deployment) ---
DEPLOYER_PRIVATE_KEY=0x<YOUR_DEPLOYER_PRIVATE_KEY>

# --- Admin Address (receives admin roles) ---
ADMIN_ADDRESS=0x<YOUR_ADMIN_ADDRESS>

# --- DAES Signer (signs EIP-712 authorizations) ---
DAES_SIGNER_ADDRESS=0x<YOUR_DAES_SIGNER_ADDRESS>

# --- Etherscan API Key (for contract verification) ---
ETHERSCAN_API_KEY=<YOUR_ETHERSCAN_API_KEY>

# --- Gas Reporter (optional) ---
REPORT_GAS=false

# DAES ETH USD Contracts - Environment Configuration
# =============================================================================
# Rename this file to .env and fill in your values
# NEVER commit .env to git!

# --- Ethereum RPC (Alchemy) ---
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>
ETH_SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>

# --- Deployer (pays gas for deployment) ---
DEPLOYER_PRIVATE_KEY=0x<YOUR_DEPLOYER_PRIVATE_KEY>

# --- Admin Address (receives admin roles) ---
ADMIN_ADDRESS=0x<YOUR_ADMIN_ADDRESS>

# --- DAES Signer (signs EIP-712 authorizations) ---
DAES_SIGNER_ADDRESS=0x<YOUR_DAES_SIGNER_ADDRESS>

# --- Etherscan API Key (for contract verification) ---
ETHERSCAN_API_KEY=<YOUR_ETHERSCAN_API_KEY>

# --- Gas Reporter (optional) ---
REPORT_GAS=false

# DAES ETH USD Contracts - Environment Configuration
# =============================================================================
# Rename this file to .env and fill in your values
# NEVER commit .env to git!

# --- Ethereum RPC (Alchemy) ---
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>
ETH_SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>

# --- Deployer (pays gas for deployment) ---
DEPLOYER_PRIVATE_KEY=0x<YOUR_DEPLOYER_PRIVATE_KEY>

# --- Admin Address (receives admin roles) ---
ADMIN_ADDRESS=0x<YOUR_ADMIN_ADDRESS>

# --- DAES Signer (signs EIP-712 authorizations) ---
DAES_SIGNER_ADDRESS=0x<YOUR_DAES_SIGNER_ADDRESS>

# --- Etherscan API Key (for contract verification) ---
ETHERSCAN_API_KEY=<YOUR_ETHERSCAN_API_KEY>

# --- Gas Reporter (optional) ---
REPORT_GAS=false

# DAES ETH USD Contracts - Environment Configuration
# =============================================================================
# Rename this file to .env and fill in your values
# NEVER commit .env to git!

# --- Ethereum RPC (Alchemy) ---
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>
ETH_SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>

# --- Deployer (pays gas for deployment) ---
DEPLOYER_PRIVATE_KEY=0x<YOUR_DEPLOYER_PRIVATE_KEY>

# --- Admin Address (receives admin roles) ---
ADMIN_ADDRESS=0x<YOUR_ADMIN_ADDRESS>

# --- DAES Signer (signs EIP-712 authorizations) ---
DAES_SIGNER_ADDRESS=0x<YOUR_DAES_SIGNER_ADDRESS>

# --- Etherscan API Key (for contract verification) ---
ETHERSCAN_API_KEY=<YOUR_ETHERSCAN_API_KEY>

# --- Gas Reporter (optional) ---
REPORT_GAS=false

# DAES ETH USD Contracts - Environment Configuration
# =============================================================================
# Rename this file to .env and fill in your values
# NEVER commit .env to git!

# --- Ethereum RPC (Alchemy) ---
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>
ETH_SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>

# --- Deployer (pays gas for deployment) ---
DEPLOYER_PRIVATE_KEY=0x<YOUR_DEPLOYER_PRIVATE_KEY>

# --- Admin Address (receives admin roles) ---
ADMIN_ADDRESS=0x<YOUR_ADMIN_ADDRESS>

# --- DAES Signer (signs EIP-712 authorizations) ---
DAES_SIGNER_ADDRESS=0x<YOUR_DAES_SIGNER_ADDRESS>

# --- Etherscan API Key (for contract verification) ---
ETHERSCAN_API_KEY=<YOUR_ETHERSCAN_API_KEY>

# --- Gas Reporter (optional) ---
REPORT_GAS=false

# DAES ETH USD Contracts - Environment Configuration
# =============================================================================
# Rename this file to .env and fill in your values
# NEVER commit .env to git!

# --- Ethereum RPC (Alchemy) ---
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>
ETH_SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>

# --- Deployer (pays gas for deployment) ---
DEPLOYER_PRIVATE_KEY=0x<YOUR_DEPLOYER_PRIVATE_KEY>

# --- Admin Address (receives admin roles) ---
ADMIN_ADDRESS=0x<YOUR_ADMIN_ADDRESS>

# --- DAES Signer (signs EIP-712 authorizations) ---
DAES_SIGNER_ADDRESS=0x<YOUR_DAES_SIGNER_ADDRESS>

# --- Etherscan API Key (for contract verification) ---
ETHERSCAN_API_KEY=<YOUR_ETHERSCAN_API_KEY>

# --- Gas Reporter (optional) ---
REPORT_GAS=false

# DAES ETH USD Contracts - Environment Configuration
# =============================================================================
# Rename this file to .env and fill in your values
# NEVER commit .env to git!

# --- Ethereum RPC (Alchemy) ---
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>
ETH_SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>

# --- Deployer (pays gas for deployment) ---
DEPLOYER_PRIVATE_KEY=0x<YOUR_DEPLOYER_PRIVATE_KEY>

# --- Admin Address (receives admin roles) ---
ADMIN_ADDRESS=0x<YOUR_ADMIN_ADDRESS>

# --- DAES Signer (signs EIP-712 authorizations) ---
DAES_SIGNER_ADDRESS=0x<YOUR_DAES_SIGNER_ADDRESS>

# --- Etherscan API Key (for contract verification) ---
ETHERSCAN_API_KEY=<YOUR_ETHERSCAN_API_KEY>

# --- Gas Reporter (optional) ---
REPORT_GAS=false
















